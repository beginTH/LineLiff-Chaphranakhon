# Rich Menu

Rich Menu routes to Branch LIFF for ordering, order history (`?screen=history`), and payment proof (`?screen=payment-proof`). Verify the LIFF ID and deployed URLs before publishing the menu.
