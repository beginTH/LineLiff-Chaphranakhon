# Archived Workflows

These JSON files are historical workflow versions kept for reference and rollback. They are not the active production workflows.

Use the files in `outputs/modular/` for the current system. Do not activate archived files together with modular workflows because webhook paths may overlap.
